[package]
name = "firework"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
tokio-rustls = "0.26.1"
log = "0.4.25"
async-trait = "0.1"

# Opcional: Dependencias de desarrollo
[dev-dependencies]
tokio-test = "0.4"
