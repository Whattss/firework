[package]
name = "bench-test"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
firework = { path = ".." }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
linkme = "0.3"
